type ToastOptions = {
  title: string
  description?: string
}

export function toast(_options: ToastOptions) {
  // Placeholder hook for visual verification only.
  // In this static ERP, we do not need a full toast system.
}

